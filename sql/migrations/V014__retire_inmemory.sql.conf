# V014 drops memory-optimized tables (DROP TABLE IF EXISTS sqlmq.[q_<name>])
# for any queue that was created with @storage='inmemory'. SQL Server
# doesn't allow DDL on memory-optimized objects inside a user transaction;
# Flyway must run this migration outside its default transaction wrapper.
executeInTransaction=false
