# V012 contains DDL on memory-optimized objects (CREATE TYPE ... WITH
# MEMORY_OPTIMIZED = ON). SQL Server does not allow such DDL inside a
# user transaction; Flyway must run this migration outside its default
# transaction wrapper.
executeInTransaction=false
